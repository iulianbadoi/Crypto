function [ prime, gen ] = generateParams(MAX)
    % Generates prime and generator for cyclic group
    
    while 1
       r = floor(rand() * MAX);
       if isprime(r)
           % Find some generator
           
       end
    end


end

